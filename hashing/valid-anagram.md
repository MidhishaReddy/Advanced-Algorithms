# valid Anagram

```
Input: s = "cat", t = "act"
Output:true
Input: s = "mat" , t="bat"
```

#### Approach

* The two strings are considered anagrams when they have same alphabets in the two strings.
*
*
